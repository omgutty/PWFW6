# Dependencies
node_modules/

# Build output
dist/

# Test results
test-results/
playwright-report/
tta-report/


.auth/*.json
#Notice .auth/*.json is gitignored — auth state files contain session tokens, 
#never commit those. The .gitkeep file keeps the empty .auth folder tracked in git.

# Environment
.env